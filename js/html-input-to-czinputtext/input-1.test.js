const TestComponent = (p) => (
  <div>
    <input
      className='inputField'
      onChange={this._onChange.bind(this, 'name')}
      placeholder="19' Scissor Lift"
      type='text'
      value={this.state.name}
    />
  </div>
)
